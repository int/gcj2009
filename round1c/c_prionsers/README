Model: `classic' DP -- Matrix Chain Multiplication / Optimal Binary Search Tree

same top-down memo algorithms:

	test.py
	test.hs
	test.cpp -- far more faster

bottom-up DP:
	dp.cpp -- faster than test.cpp by a factor of 2
	dp.hs -- using IOArray: faster than test.hs but slower than dp.cpp
